// Commute Mode requires a native dev build (uses @react-native-voice/voice).
// This stub keeps Expo Go from crashing on module load.
// When ready: replace this file with the full implementation from git history.
//
// Expo Router treats every file under app/ as a route and requires a default
// export. Without one, the production build hard-crashes at startup. This
// no-op component satisfies the route contract while doing nothing.
export default function CommuteImplStub() {
  return null;
}
